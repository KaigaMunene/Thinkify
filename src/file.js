// File: example.js

var foo = "bar" // 'var' is not preferred; should use 'let' or 'const'
// Missing semicolon; should end with ';'

function greet(name) {
    console.log('Hello, ' + name)
    // Missing semicolon; should end with ';'
}

greet('World')
